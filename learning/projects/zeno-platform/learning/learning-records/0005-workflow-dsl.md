# 0005 · Workflow DSL + 节点执行器
**日期**：2026-09-01  
**状态**：已学（第 5 课）

我已经把工作流理解成“图 + 节点执行器 + 变量池”，而不是单纯的脚本。最重要的底层事实是：`workflow_dsl.py` 负责定义节点合法性与图结构校验，`workflow_node_executors.py` 负责把每种 `type` 映射到真正执行逻辑，真正跑起来时是按 `edges` 遍历图而不是按顺序源码执行。这个认知改变了后续排障：一旦工作流看起来“能画但不执行”，要先查图结构和节点字段，而不是直接怀疑 LLM。